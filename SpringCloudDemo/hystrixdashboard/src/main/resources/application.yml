server:
  port: 5561

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:5555/eureka/

spring:
  application:
    name: hystrixdashboard

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
      base-path: /